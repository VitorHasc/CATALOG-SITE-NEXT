generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = "mysql://USER:PASSWORD@localhost:3306/NOME_DO_BANCO"
}

model User {
  id       Int      @id @default(autoincrement())
  email    String   @unique
  password String
}
